type products = {
  id: string;
  title: string;
  price: number;
  rating: number;
  thumbnail: string;
  description: string;
  brand: string;
  category: string;
};

export default products;
